<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.onthi.laixe.onthigiaypheplaixe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.DeThiActivity" />
        <activity android:name=".activity.ShowKQNgauNhienActivity" />
        <activity android:name=".activity.ScreenSlideActivity" />
        <activity android:name=".activity.ShowKQActivity" />
        <activity android:name=".activity.ChuDeActivity" />
        <activity android:name=".activity.OnTapActivity" />
        <activity android:name=".activity.NgauNhienActivity" />
        <activity android:name=".activity.ScreenSlideChuDeActivity" />
        <activity android:name=".activity.BienBaoActivity" />
        <activity android:name=".activity.MeoActivity"></activity>
    </application>

</manifest>